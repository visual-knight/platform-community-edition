import { ImageStorePipe } from './image-store.pipe';

describe('ImageStorePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageStorePipe();
    expect(pipe).toBeTruthy();
  });
});
