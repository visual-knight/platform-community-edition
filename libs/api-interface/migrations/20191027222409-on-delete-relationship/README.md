# Migration `20191027222409-on-delete-relationship`

This migration has been generated by Mitko Tschimev at 10/27/2019, 10:24:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190804225624-remove-user-project-dependency..20191027222409-on-delete-relationship
--- datamodel.dml
+++ datamodel.dml
@@ -4,15 +4,15 @@
 }
 generator photon {
   provider  = "photonjs"
-  platforms = ["native", "linux-glibc-libssl1.0.2"]
+  binaryTarget = ["native", "linux-glibc-libssl1.0.2"]
   output    = "../../node_modules/@generated/photonjs"
 }
 generator photon_layer {
   provider  = "photonjs"
-  platforms = ["linux-glibc-libssl1.0.2"]
+  binaryTarget = ["linux-glibc-libssl1.0.2"]
   output    = "../../layers/photon/nodejs/node_modules/@generated/photonjs"
 }
 model User {
@@ -35,9 +35,9 @@
   updatedAt DateTime @updatedAt
   name        String
   description String?
-  tests       Test[]
+  tests       Test[] @relation(onDelete: CASCADE)
 }
 model Test {
   id        String   @id @default(cuid())
@@ -45,9 +45,9 @@
   updatedAt DateTime @updatedAt
   name       String
   project    Project
-  variations Variation[]
+  variations Variation[] @relation(onDelete: CASCADE)
 }
 model Variation {
   id        String   @id @default(cuid())
@@ -57,9 +57,9 @@
   browserName String
   deviceName  String
   test        Test
-  testSessions   TestSession[]
+  testSessions   TestSession[] @relation(onDelete: CASCADE)
   additionalData String?
   baseline       TestSession?  @relation(name: "VariationBaseline")
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191027222409-on-delete-relationship)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191027222409-on-delete-relationship'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
