# Migration `watch-20191027170943`

This migration has been generated by Mitko Tschimev at 10/27/2019, 5:10:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191024194144..watch-20191027170943
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("VK_POSTGRES_URL")
 }
 generator photon {
   provider  = "photonjs"
@@ -35,9 +35,9 @@
   updatedAt DateTime @updatedAt
   name        String
   description String?
-  tests       Test[]
+  tests       Test[] @relation(onDelete: CASCADE)
 }
 model Test {
   id        String   @id @default(cuid())
@@ -45,9 +45,9 @@
   updatedAt DateTime @updatedAt
   name       String
   project    Project
-  variations Variation[]
+  variations Variation[] @relation(onDelete: CASCADE)
 }
 model Variation {
   id        String   @id @default(cuid())
@@ -57,9 +57,9 @@
   browserName String
   deviceName  String
   test        Test
-  testSessions   TestSession[]
+  testSessions   TestSession[] @relation(onDelete: CASCADE)
   additionalData String?
   baseline       TestSession?  @relation(name: "VariationBaseline")
 }
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191027170943)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191027170943'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
